<Window x:Class="mouham_cWpfMedecin.View.LoginView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:behaviours="clr-namespace:mouham_cWpfMedecin"
        Height="200" Width="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <TextBlock Text="Username" Grid.Row="0" Grid.Column="0" VerticalAlignment="Center" HorizontalAlignment="Center" />
        <TextBox Text="{Binding Login, Mode=TwoWay}" Grid.Row="0" Grid.Column="1" Height="20" Padding="0" />

        <TextBlock Text="Password" Grid.Row="1" Grid.Column="0" HorizontalAlignment="Center" VerticalAlignment="Center" />
        <TextBox  Text="{Binding Password, Mode=TwoWay}" Grid.Row="1" Grid.Column="1"  Height="20" />
           
        <Button Content="Log in" Command="{Binding LoginCommand}" Width="100" Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="2" Height="28" />
        <Grid.Style>
            <Style>
                <Style.Triggers>
                    <DataTrigger  Binding="{Binding CloseTrigger}" Value="True">
                        <Setter Property="behaviours:WindowCloseBehaviour.Close" Value="True"/>
                    </DataTrigger>
                </Style.Triggers>
            </Style>
            </Grid.Style>
    </Grid>
</Window>
